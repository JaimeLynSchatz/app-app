<div class='row'>
  <div class='col-lg-12'>
    <legend>Profile</legend>
    <%= form_for current_user, method: :patch do |f| -%>
      <%= render "form", f: f %>
      <%= f.submit "Update", class: 'btn btn-success' %>
    <% end -%>    
  </div>
</div>
